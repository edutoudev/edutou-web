# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# Public variables (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key-here

# Server-only variables (NEVER expose to client - keep in .env.local)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# How to get these values:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy:
#    - Project URL → NEXT_PUBLIC_SUPABASE_URL
#    - anon public → NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
#    - service_role → SUPABASE_SERVICE_ROLE_KEY (⚠️ Keep secret!)

# Note: The service_role key bypasses Row Level Security (RLS)
# NEVER expose it to the client or commit it to version control!
